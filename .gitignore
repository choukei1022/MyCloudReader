# 依赖包 (绝对不能传)
node_modules/
server/node_modules/

# 本地环境配置 (包含密码，绝对不能传)
.env
.env.local

# 你的私人书库、字体和缓存 (千万别传，否则仓库瞬间撑爆)
books/
fonts/
*.db
*.db-wal
*.db-shm

# 前端编译产物 (别人下载源码后自己编译即可)
dist/

# 日志与系统垃圾
*.log
.DS_Store
Thumbs.db